# Advanced 01: Remote state

Remote state enables team collaboration and state locking. Replace the backend placeholders with environment-specific values before `terraform init`.

Never commit access keys or storage secrets. Prefer Azure AD/workload identity and least-privilege access.
