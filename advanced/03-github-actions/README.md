# Advanced 03: Terraform in GitHub Actions

A manual workflow template for format, init, validate, plan, and optional apply. Copy it into a real repository and update `working-directory`, credentials, environments, approvals, and backend configuration.

Use protected environments and approval gates for production apply operations.
